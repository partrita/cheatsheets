# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: typst-compile-check
        name: Typst Compilation Check
        entry: ./scripts/typst_build.sh check
        language: system
        files: '\\.typ$'
        pass_filenames: false
        always_run: true
