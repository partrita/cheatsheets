#import "./templates/conf.typ": *

#show: template.with(
  title: "Windows 치트시트",
  header: [#datetime.today().display()],
  footer: "",
)

= 1. 파일 및 디렉토리 관리

- `dir`: 현재 디렉토리의 파일 및 폴더 목록을 표시합니다.
  - `/a:[attributes]`: 특정 속성의 파일을 표시합니다. (예: `/ah` 숨김 파일, `/ad` 디렉토리)
  - `/o:[sortorder]`: 지정된 순서로 정렬합니다. (예: `/on` 이름순, `/os` 크기순, `/od` 날짜순)
  - `/b`: 기본 형식(경로 정보 없음)으로 표시합니다.
  - `/s`: 하위 디렉토리의 파일까지 모두 표시합니다.
- `cd` 또는 `chdir`: 현재 작업 디렉토리를 변경하거나 표시합니다.
  - `cd ..`: 상위 디렉토리로 이동합니다.
  - `cd C:\Users`: 특정 경로로 이동합니다.
  - `cd`: 현재 디렉토리 경로를 표시합니다.
  - `cd /d [drive:][path]`: 드라이브와 디렉토리를 한 번에 변경합니다.
- `mkdir` 또는 `md`: 새 디렉토리(폴더)를 생성합니다.
  - `mkdir C:\A\B\C`: 중간 디렉토리(A, B)가 없으면 오류가 발생합니다.
- `rmdir` 또는 `rd`: 비어있는 디렉토리를 삭제합니다.
  - `/s`: 지정된 디렉토리와 그 안의 모든 파일 및 하위 디렉토리를 삭제합니다.
  - `/q`: 조용한 모드로, `/s` 옵션과 함께 사용할 때 삭제 확인 프롬프트를 표시하지 않습니다.
- `del` 또는 `erase`: 하나 이상의 파일을 삭제합니다.
  - `/p`: 각 파일을 삭제하기 전에 확인 메시지를 표시합니다.
  - `/f`: 읽기 전용 파일도 강제로 삭제합니다.
  - `/s`: 지정된 디렉토리와 모든 하위 디렉토리에서 파일을 삭제합니다.
  - `/q`: 조용한 모드.
- `copy`: 파일을 다른 위치로 복사합니다.
  - `copy file1.txt + file2.txt newfile.txt`: 여러 파일을 하나로 합칩니다.
  - `/y`: 대상 파일이 이미 있을 경우 덮어쓸지 묻지 않습니다.
- `xcopy`: 파일과 디렉토리 트리를 복사합니다.
  - `/s`: 비어 있지 않은 디렉토리와 하위 디렉토리를 복사합니다.
  - `/e`: 비어 있는 디렉토리를 포함하여 모든 하위 디렉토리를 복사합니다.
  - `/h`: 숨김 파일과 시스템 파일도 복사합니다.
- `robocopy`: 강력한 파일 복사 유틸리티입니다.
  - `robocopy <source> <destination> /mir`: 원본과 대상을 미러링합니다. (원본에 없는 파일은 대상에서 삭제)
- `move`: 파일을 다른 위치로 이동합니다.
- `ren` 또는 `rename`: 파일 또는 디렉토리의 이름을 변경합니다.
- `type`: 텍스트 파일의 내용을 표시합니다.
- `find`: 파일에서 텍스트 문자열을 검색합니다.
- `findstr`: `find`보다 강력한 문자열 검색 도구, 정규 표현식 지원.



= 2. 시스템 정보 및 네트워크 관리

- `cls`: 명령 프롬프트 화면을 지웁니다.
- `systeminfo`: 컴퓨터의 시스템 정보를 자세히 표시합니다.
- `ver`: Windows 버전을 표시합니다.
- `driverquery`: 설치된 장치 드라이버 목록과 속성을 표시합니다.
  - `-v`: 상세 정보를 표시합니다.
- `getmac`: 컴퓨터의 MAC 주소를 표시합니다.
- `whoami`: 현재 로그온한 사용자의 정보를 표시합니다. (`/groups` 그룹 정보, `/priv` 권한 정보)
- `ipconfig`: 모든 네트워크 어댑터의 IP 주소 구성을 표시합니다.
  - `/all`: 모든 어댑터의 상세 구성(MAC 주소, DNS 서버 등)을 표시합니다.
  - `/release`: IP 주소를 해제합니다.
  - `/renew`: IP 주소를 갱신합니다.
  - `/flushdns`: DNS 확인자 캐시를 비웁니다.
- `ping <host>`: 다른 컴퓨터와의 네트워크 연결을 테스트합니다.
  - `-t`: 중지할 때까지 계속 ping을 보냅니다.
  - `-n <count>`: 지정된 횟수만큼 ping을 보냅니다.
- `tracert <host>`: 대상 호스트까지의 경로를 추적하고 각 홉의 지연 시간을 표시합니다.
- `pathping <host>`: `ping`과 `tracert`의 기능을 합친 것으로, 경로의 각 노드에서 패킷 손실 정보를 제공합니다.
- `netstat`: 활성 TCP 연결, 수신 대기 중인 포트, 이더넷 통계 등을 표시합니다.
  - `-a`: 모든 연결 및 수신 대기 포트를 표시합니다.
  - `-n`: 주소와 포트 번호를 숫자 형식으로 표시합니다.
  - `-o`: 각 연결의 소유자 프로세스 ID(PID)를 표시합니다.
  - `-b`: 각 연결을 만든 실행 파일을 표시합니다. (관리자 권한 필요)
- `nslookup <domain>`: DNS 서버에 쿼리하여 도메인 이름이나 IP 주소 정보를 얻습니다.

= 3. 프로세스 및 서비스 관리

- `tasklist`: 현재 실행 중인 모든 프로세스 목록을 표시합니다.
  - `/svc`: 각 프로세스에서 호스팅하는 서비스를 표시합니다.
  - `/m <module>`: 특정 DLL 모듈을 사용하는 프로세스를 찾습니다.
- `taskkill`: 이름이나 프로세스 ID(PID)로 프로세스를 종료합니다.
  - `/pid <processid>`: 프로세스 ID로 종료합니다.
  - `/im <imagename>`: 이미지 이름으로 종료합니다.
  - `/f`: 프로세스를 강제로 종료합니다.
  - `/t`: 지정된 프로세스와 그 자식 프로세스들을 모두 종료합니다.
- `sc`: 서비스 제어 관리자와 통신하는 명령어입니다.
  - `sc query`: 모든 서비스의 상태를 표시합니다.
  - `sc query <servicename>`: 특정 서비스의 상태를 표시합니다.
  - `sc start <servicename>`: 서비스를 시작합니다.
  - `sc stop <servicename>`: 서비스를 중지합니다.
  - `sc config <servicename> start= <boot|system|auto|demand|disabled>`: 서비스의 시작 유형을 변경합니다.
- `sfc /scannow`: 시스템 파일의 무결성을 검사하고 손상된 파일을 복구합니다.
- `chkdsk`: 디스크의 파일 시스템 오류를 검사하고 수정합니다.
  - `/f`: 디스크의 오류를 고칩니다.
  - `/r`: 불량 섹터를 찾고 읽을 수 있는 정보를 복구합니다.

= 4. 기타 유용한 명령어

- `powershell`: PowerShell 프롬프트를 시작합니다.
- `runas /user:<username> <command>`: 다른 사용자 권한으로 명령을 실행합니다.
- `gpupdate /force`: 그룹 정책 설정을 강제로 업데이트합니다.
- `shutdown`: 컴퓨터를 종료하거나 다시 시작합니다.
  - `/s`: 컴퓨터를 종료합니다.
  - `/r`: 컴퓨터를 다시 시작합니다.
  - `/t <seconds>`: 지정된 시간(초) 후에 종료/재시작합니다.
  - `/a`: 예약된 종료/재시작을 취소합니다.
  - `/hybrid`: 빠른 시작을 위해 컴퓨터를 종료하고 준비합니다.
- `assoc`: 파일 확장자와 파일 형식 간의 연결을 표시하거나 수정합니다.
- `ftype`: 파일 형식과 연결된 열기 명령을 표시하거나 수정합니다.
- `powercfg`: 전원 설정을 구성하고 절전 모드를 제어합니다.
  - `powercfg /list`: 모든 전원 관리 옵션을 나열합니다.
  - `powercfg /hibernate on|off`: 최대 절전 모드를 켜거나 끕니다.
  - `powercfg /energy`: 60초 동안 시스템 동작을 분석하여 에너지 효율성 문제를 보고합니다.
- `wevtutil`: 이벤트 로그에서 정보를 검색합니다.
  - `wevtutil qe System /c:1 /rd:true /f:text`: 시스템 로그에서 가장 최근 이벤트를 텍스트 형식으로 표시합니다.
