#import "../templates/conf.typ": *

#show: template.with(
  title: "lf (list files) Cheatsheet",
  header: [#datetime.today().display()],
  footer: "generated by Gemini CLI",
)

= Overview and Core Concepts

`lf` is a terminal file manager written in Go, inspired by `ranger`. It uses a server-client architecture to share state between multiple instances and provides Vim-like keybindings for efficient file system navigation.

= Navigation and Movement

- `k` or `↑`: Move up.
- `j` or `↓`: Move down.
- `h` or `←`: Move to parent directory.
- `l` or `→`: Open file or enter directory.
- `gg`: Move to top of the list.
- `G`: Move to bottom of the list.
- `H`, `M`, `L`: Move to high (top), middle, or low (bottom) of the screen.
- `~` or `gh`: Change directory to home (`$HOME`).

= File and Directory Management

- `Space`: Select or unselect current item (for batch operations).
- `d`: Move selected items to trash (`~/.local/share/lf/trash` typically).
- `D`: Delete selected items permanently.
- `y`: Yank (copy) selected items.
- `c`: Cut selected items.
- `p`: Paste yanked or cut items to current directory.
- `r`: Rename selected item.
- `:mkdir <name>`: Create new directory.
- `:touch <name>`: Create new empty file.

= Search and Filtering

- `/`: Search mode (incremental filtering as you type).
- `f`: Filter mode (pattern based).
- `?`: Browse previous search command history.

= Shell Integration and Commands

- `!`: Execute the command in the shell (e.g. `!zip -r archive.zip folder`).
- `$`: Open a shell prompt in the current directory (exit shell to return to `lf`).
- `c`: Clear command line or jump to client mode for scripting interactions.

= View Options and Sorting

- `zh`: Toggle visibility of hidden files.
- `zr`: Reverse the sorting order.
- `zs`: Change sort criteria (n: name, s: size, t: time).
- `zp`: Toggle preview pane display.

= Configuration and Customization

Configuration file location: `~/.config/lf/lfrc`.
- *Key Mapping*: `map gh cd ~`.
- *Custom Command*: `cmd my-command echo "hello"`.
- *Previewer Settings*:
  ```bash
  # In lfrc
  set previewer "/path/to/preview_script.sh"
  ```

= Terminal Multiplexing (tmux/zellij)

Because of its server-client architecture, `lf` works exceptionally well with multiplexers. You can yank a file in one pane and paste it into another running instance of `lf` in real-time.


= Pro Tips

== Directory Sync on Exit
To make your shell change directory to the last visited path when exiting `lf`, use a wrapper function like this in your `.bashrc` or `.zshrc`:
```bash
lfcd () {
    tmp="$(mktemp)"
    lf -last-dir-path="$tmp" "$@"
    if [ -f "$tmp" ]; then
        dir="$(cat "$tmp")"
        rm -f "$tmp"
        if [ -d "$dir" ]; then
            if [ "$dir" != "$(pwd)" ]; then
                cd "$dir"
            fi
        fi
    fi
}
# Use 'lfcd' instead of 'lf' to navigate.
```

== Rapid Selection
Use `v` in conjunction with movement to quickly select a range of files for batch processing.
