#import "../templates/conf.typ": *

#show: template.with(
  title: "Yazi Cheatsheet",
  header: [#datetime.today().display()],
  footer: "generated by Gemini CLI",
)

= Basic Navigation

Yazi is a modern, terminal-based file manager written in Rust, featuring high performance and rich previews.

- `j`, `k`: Move down/up.
- `h`, `l`: Move to parent directory / enter directory.
- `Enter`: Open the selected file or directory.
- `0`, `$`: Move to the first/last item in the list.
- `g g`, `G`: Move to beginning/end of list.
- `<C-u>`, `<C-d>`: Scroll half-page up/down.
- `f`: Search (fuzzy find).

= File Operations

- `a`: Create a new file or directory (use `/` at the end for directories).
- `r`: Rename item.
- `d`: Delete (move to trash).
- `x`: Cut.
- `y`: Copy (Yank).
- `p`: Paste.
- `K`: Create a symbolic link.
- `Space`: Select (visual multi-select).
- `v`: Toggle visual select mode.

= View and Layout

- `~`: Open the Help menu.
- `w`: Open Tasks manager.
- `[`, `]`: Switch between tabs.
- `1`, `2`, ..., `9`: Jump to specific tab.
- `t`: Create a new tab.
- `q`: Quit Yazi.

= Config and Shell

- `: `: Enter command mode.
- `! `: Execute shell command in a subshell.
- `c`: Open the Config directory.
- `z`: Quick jump using zoxide (if installed).

= Filtering and Sorting

- `/`: Start searching.
- `,`: Change sorting order (Name, Size, Mtime, etc.).
- `.`: Toggle hidden files visibility.

= Advanced Usage

== Image Previews
Yazi supports high-speed image previews in various terminals using its proprietary protocols.

== Task Management
Operations like large copies or deletes run in the background. Use `w` to view progress and manage tasks.

== Custom Bindings
Configurable via `keymap.toml`. Support for complex multi-key sequences and shell scripts.
