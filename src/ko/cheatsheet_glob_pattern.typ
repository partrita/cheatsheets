#import "../templates/conf.typ": *

#show: template.with(
  title: "Glob 패턴 치트시트",
  header: [#datetime.today().display()],
  footer: "generated by Gemini CLI",
)

= 1. Glob 패턴 개요

Glob 패턴은 와일드카드 문자를 사용하여 하나 이상의 파일 또는 디렉토리 경로를 효율적으로 지정하는 방법입니다. 주로 셸(Shell) 환경에서 파일 목록을 확장하거나 특정 파일들을 한꺼번에 조작할 때 사용됩니다.

= 2. 기본 와일드카드

- `*`: 0개 이상의 모든 문자와 일치합니다.
  - 예: `*.txt` -> `.txt` 확장자를 가진 모든 파일 (예: `memo.txt`, `log.txt`)
  - 예: `img*` -> `img`로 시작하는 모든 파일 및 폴더 (예: `img1.png`, `images/`)
- `?`: 정확히 1개의 모든 문자와 일치합니다.
  - 예: `file?.log` -> `file1.log`, `fileA.log` 등과 일치하며 `file10.log`와는 일치하지 않습니다.
- `[abc]`: 대괄호 안에 나열된 문자 중 하나와 일치합니다.
  - 예: `data[12].csv` -> `data1.csv` 또는 `data2.csv`와 일치합니다.
- `[a-z]`: 지정된 범위 내의 문자 중 하나와 일치합니다.
  - 예: `[0-9].log` -> 숫자로 된 한 글자 파일명과 일치합니다.
- `[!abc]` 또는 `[^abc]`: 나열된 문자에 포함되지 않은 문자와 일치합니다. (셸에 따라 기호가 다를 수 있음)

= 3. 확장 및 고급 패턴

- `**`: 현재 디렉토리 및 모든 하위 디렉토리를 재귀적으로 탐색합니다. (최신 셸 환경에서 지원)
  - 예: `**/README.md` -> 프로젝트 내 모든 경로에 있는 `README.md` 파일을 찾습니다.
- `{}` (중괄호 확장): 쉼표로 구분된 여러 문자열 조합을 생성합니다. (엄밀히는 셸의 기능이지만 자주 병행됨)
  - 예: `mv file.{txt,md}` -> `mv file.txt file.md` 명령으로 해석됩니다.
  - 예: `src/*.{js,ts}` -> `src` 디렉토리 내의 모든 JS 및 TS 파일을 선택합니다.

= 4. 주요 사용 예시

- 특정 확장자 파일 일괄 조회:
  ```bash
  ls *.{jpg,png,gif}
  ```
- 하위 디렉토리 내 특정 언어 소스 코드 검색:
  ```bash
  ls src/**/*.ts
  ```
- 숫자 패턴이 포함된 테스트 파일 찾기:
  ```bash
  ls test[0-9]*.py
  ```
- 숨김 파일을 제외한 모든 항목 선택:
  ```bash
  ls *
  ```
- 모든 숨김 파일 및 일반 파일 선택:
  ```bash
  ls .* *
  ```

= 5. 셸별 유의 사항

Glob 패턴의 세부 동작은 사용하는 셸(Bash, Zsh, Fish 등)에 따라 다를 수 있습니다. 예를 들어 Bash에서는 `**` 패턴을 활성화하기 위해 `shopt -s globstar` 설정이 필요할 수 있으며, 셸 설정에 따라 특수 문자를 이스케이프(`\`) 처리해야 할 수도 있습니다.
