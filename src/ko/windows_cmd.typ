#import "../templates/conf.typ": *

#show: template.with(
  title: "Windows CMD 치트시트",
  header: [#datetime.today().display()],
  footer: "가이드: 명령 프롬프트 필수 명령어",
)

= 파일 및 디렉토리 관리

- `dir`: 현재 디렉토리의 파일 및 폴더 목록을 출력합니다.
  - `/a:[특성]`: 지정한 속성(예: `/ah` 숨김 파일, `/ad` 디렉토리)의 항목만 표시합니다.
  - `/o:[정렬]`: 지정한 순서(예: `/on` 이름순, `/os` 크기순, `/od` 날짜순)로 정렬하여 보여줍니다.
  - `/b`: 최소한의 정보(파일명)만 간단히 표시합니다.
  - `/s`: 현재 디렉토리뿐만 아니라 모든 하위 디렉토리의 파일까지 검색하여 표시합니다.
- `cd` 또는 `chdir`: 현재 작업 디렉토리를 변경하거나 확인합니다.
  - `cd ..`: 상위 디렉토리로 이동합니다.
  - `cd C:\Users`: 특정 절대 경로로 이동합니다.
  - `cd`: 현재 위치한 디렉토리의 경로를 출력합니다.
  - `cd /d [드라이브:][경로]`: 드라이브와 디렉토리를 동시에 변경할 때 사용합니다.
- `mkdir` 또는 `md`: 새로운 디렉토리를 생성합니다.
  - `mkdir C:\A\B\C`: 중간 경로(A, B)가 존재하지 않으면 오류가 발생하므로 주의가 필요합니다.
- `rmdir` 또는 `rd`: 디렉토리를 삭제합니다. (기본적으로 비어있는 디렉토리만 삭제 가능)
  - `/s`: 디렉토리 내부에 파일이나 하위 폴더가 있어도 모두 강제로 삭제합니다.
  - `/q`: 삭제 시 사용자 확인 메시지를 표시하지 않는 '조용한 모드'로 동작합니다.
- `del` 또는 `erase`: 하나 이상의 파일을 삭제합니다.
  - `/p`: 파일을 삭제하기 전에 매번 사용자 확인을 거칩니다.
  - `/f`: 읽기 전용 파일도 강제로 삭제합니다.
  - `/s`: 하위 디렉토리까지 뒤져서 일치하는 모든 파일을 삭제합니다.
- `copy`: 파일을 다른 위치로 복사하거나 여러 파일을 하나로 합칩니다.
  - `copy file1.txt + file2.txt newfile.txt`: 두 파일을 합쳐 새로운 파일을 생성합니다.
  - `/y`: 대상 파일이 이미 존재하더라도 묻지 않고 덮어씁니다.
- `xcopy`: 파일뿐만 아니라 디렉토리 구조 전체를 복사하는 향상된 도구입니다.
  - `/s`: 비어 있지 않은 하위 디렉토리까지 모두 복사합니다.
  - `/e`: 비어 있는 디렉토리까지 포함하여 구조 전체를 복사합니다.
  - `/h`: 숨김 파일이나 시스템 파일도 함께 복사합니다.
- `robocopy`: 대량의 파일이나 네트워크 복사에 최적화된 강력한 유틸리티입니다.
  - `robocopy <원본> <대상> /mir`: 원본과 대상을 완벽히 미러링합니다. (원본에 없는 파일은 대상에서도 삭제)
- `move`: 파일을 다른 위치로 옮기거나 파일 이름을 변경합니다.
- `ren` 또는 `rename`: 파일이나 디렉토리의 이름을 바꿉니다.
- `type`: 텍스트 파일의 내용을 화면에 바로 출력합니다.
- `find`: 파일 내에서 특정 텍스트 문자열을 검색합니다.
- `findstr`: `find`보다 강력한 검색 기능을 제공하며, 정규 표현식을 지원합니다.



= 시스템 정보 및 네트워크 관리

- `cls`: 명령 프롬프트 화면의 모든 텍스트를 지우고 깨끗하게 정리합니다.
- `systeminfo`: 운영체제 설정, 하드웨어 사양 등 시스템의 상세 정보를 조회합니다.
- `ver`: 현재 사용 중인 Windows 버전을 확인합니다.
- `driverquery`: 시스템에 설치된 장치 드라이버 목록과 상태를 표시합니다.
- `whoami`: 현재 로그인한 사용자 정보를 확인합니다. (`/groups` 그룹 정보, `/priv` 보유 권한 조회)
- `ipconfig`: 네트워크 어댑터의 IP 주소 구성을 확인합니다.
  - `/all`: MAC 주소, DNS 서버 등 모든 상세 네트워크 정보를 표시합니다.
  - `/release` / `/renew`: IP 주소를 해제하거나 다시 할당받습니다.
  - `/flushdns`: DNS 확인자 캐시를 비워 네트워크 문제를 해결합니다.
- `ping <호스트>`: 특정 서버나 컴퓨터와의 연결 상태를 테스트합니다.
- `tracert <호스트>`: 목적지까지 패킷이 거쳐가는 경로를 추적하여 지연 구간을 확인합니다.
- `pathping <호스트>`: `ping`과 `tracert`의 기능을 결합하여 경로상의 패킷 손실률을 분석합니다.
- `netstat`: 활성 네트워크 연결, 포트 상태, 이더넷 통계 등을 조회합니다.
  - `-a`: 모든 연결 및 대기 중인 포트를 표시합니다.
  - `-n`: 주소와 포트 번호를 숫자 형식으로 표시하여 속도를 높입니다.
  - `-o`: 각 연결을 생성한 프로세스의 ID(PID)를 함께 보여줍니다.
- `nslookup <도메인>`: DNS 서버에 질의하여 도메인의 IP 주소 및 DNS 레코드를 조회합니다.

= 프로세스 및 서비스 관리

- `tasklist`: 현재 실행 중인 모든 프로세스의 목록과 사용 리소스를 보여줍니다.
  - `/svc`: 프로세스별로 구동 중인 서비스 이름을 표시합니다.
- `taskkill`: 실행 중인 프로세스를 종료합니다.
  - `/pid <ID>`: 프로세스 ID를 지정하여 종료합니다.
  - `/im <이름>`: 실행 파일 이름을 지정하여 종료합니다.
  - `/f`: 응답하지 않는 프로세스를 강제로 종료합니다.
- `sc`: 시스템 서비스 제어 및 관리를 수행합니다.
  - `sc query`: 모든 서비스의 현재 상태를 확인합니다.
  - `sc start/stop <서비스명>`: 서비스를 시작하거나 중지합니다.
  - `sc config <서비스명> start= <유형>`: 서비스의 시작 유형(자동, 수동, 사용 안 함 등)을 변경합니다.
- `sfc /scannow`: 시스템 파일의 손상을 검사하고 원본 파일로 복구합니다.
- `chkdsk`: 하드 디스크의 파일 시스템 오류를 검사하고 수리합니다.
  - `/f`: 발견된 오류를 즉시 수정합니다.
  - `/r`: 배드 섹터를 검사하고 읽기 가능한 데이터를 복구합니다.

= 기타 유용한 명령어

- `powershell`: 더욱 강력한 자동화 기능을 제공하는 PowerShell 셸을 시작합니다.
- `runas /user:<계정> <명령어>`: 다른 사용자 권한(보통 관리자 권한)으로 특정 명령을 실행합니다.
- `gpupdate /force`: 변경된 그룹 정책(Group Policy) 설정을 즉시 강제로 적용합니다.
- `shutdown`: 컴퓨터를 종료하거나 재시작하는 명령을 내립니다.
  - `/s`: 컴퓨터를 완전히 종료합니다.
  - `/r`: 시스템을 다시 시작합니다.
  - `/t <초>`: 지정한 시간(초) 후에 명령을 수행하도록 예약합니다.
  - `/a`: 예약된 종료/재시작 명령을 취소합니다.
- `assoc`: 특정 파일 확장자와 연결된 프로그램 종류를 확인하거나 수정합니다.
- `ftype`: 파일 형식과 연결된 실행 명령어를 정의합니다.
- `powercfg`: 전원 설정 및 절전 모드 관련 옵션을 제어합니다.
- `wevtutil`: Windows 이벤트 로그에서 특정 조건의 정보를 검색하거나 관리합니다.
